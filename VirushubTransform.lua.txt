local TweenService = game:GetService("TweenService")
local GuiService = game:GetService("GuiService")

local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MaintenanceUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Main Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 420, 0, 120)
frame.Position = UDim2.new(0.5, -210, 0.5, -60)
frame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
frame.BackgroundTransparency = 0.15
frame.BorderSizePixel = 0
frame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 14)
corner.Parent = frame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(80, 255, 180)
stroke.Thickness = 1.5
stroke.Transparency = 0.2
stroke.Parent = frame

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 35)
title.Position = UDim2.new(0, 0, 0, 8)
title.BackgroundTransparency = 1
title.Text = "⚠ السكربت تحت الصيانة | Script Maintenance"
title.Font = Enum.Font.GothamBold
title.TextSize = 20
title.TextColor3 = Color3.fromRGB(255,255,255)
title.Parent = frame

-- Description
local desc = Instance.new("TextLabel")
desc.Size = UDim2.new(1, -20, 0, 40)
desc.Position = UDim2.new(0, 10, 0, 45)
desc.BackgroundTransparency = 1
desc.TextWrapped = true
desc.Text = "تابع سيرفرنا بالدسكورد حتى توصلك جميع تحديثات السكربت أول بأول."
desc.Font = Enum.Font.Gotham
desc.TextSize = 15
desc.TextColor3 = Color3.fromRGB(200,200,200)
desc.Parent = frame

-- Discord Button
local button = Instance.new("TextButton")
button.Size = UDim2.new(0, 170, 0, 28)
button.Position = UDim2.new(0.5, -85, 1, -38)
button.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
button.Text = "📋 نسخ رابط الدسكورد"
button.Font = Enum.Font.GothamBold
button.TextSize = 14
button.TextColor3 = Color3.fromRGB(255,255,255)
button.Parent = frame

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 8)
buttonCorner.Parent = button

-- Pulse Animation
local tweenInfo = TweenInfo.new(
	1,
	Enum.EasingStyle.Sine,
	Enum.EasingDirection.InOut,
	-1,
	true
)

local pulse = TweenService:Create(title, tweenInfo, {
	TextTransparency = 0.35
})

pulse:Play()

-- Copy Discord Link
button.MouseButton1Click:Connect(function()
	if setclipboard then
		setclipboard("https://discord.gg/gHYTPfQtVy")
		button.Text = "✔ تم نسخ الرابط"
		
		task.wait(2)
		button.Text = "📋 نسخ رابط الدسكورد"
	else
		button.Text = "❌ النسخ غير مدعوم"
		
		task.wait(2)
		button.Text = "📋 نسخ رابط الدسكورد"
	end
end)

-- Smooth Pop In
frame.Size = UDim2.new(0, 0, 0, 0)

TweenService:Create(
	frame,
	TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
	{
		Size = UDim2.new(0, 420, 0, 120)
	}
):Play()
